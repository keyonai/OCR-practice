# OCR-practice
Practice OCRs (tesseractocr, paddleocr, and easyocr) and use bounding boxes
uses opencv, pymupdf, numpy, pillow, regex, matplotlib
There is an error with paddlrocr because of compatibility issues with Python 3.12
